<!DOCTYPE html>
<html lang="en">
<head>
  <title>Planner de Estudio</title>
  <meta charset="utf-8">
  <meta name="viewport" conten="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.9.3/css/bulma.min.css">
</head>
<body>
  <section class="section">
    <div class="container">
      <h1 class="title">Agrega tus Contenidos</h1>

      <% if @contenido && @contenido.errors.any? %>
        <div class="notification is-danger">
          <button class="delete"></button>
          <strong>Error al insertar el contenido:</strong>
          <ul>
            <% @contenido.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form_with model: @contenido, url: "/add_content", class: "box" do |f| %>

        <div class="field">
          <label class="label">Nombre del contenido</label>
          <div class="control">
            <%= f.text_field :nombre, name: "contenido[nombre]", class: "input" %>
          </div>
        </div>

        <div class="field">
          <label class="label">Descripción</label>
          <div class="control">
            <%= f.text_area :descripcion, name: "contenido[descripcion]", class: "textarea", rows: 4 %>
          </div>
        </div>

        <div class="botones" style="display: flex; justify-content: space-between;">
          <div class="agregar" style="float: left;">
            <%= link_to "Agregar Contenido", "/add_content", class: "button is-info", data: { turbo_method: :post } %>
          </div>
          <div class="crear_form" style="float: right;">
            <%= link_to "Crear Planner", "/crear_planner", class: "button is-primary", data: { turbo_method: :post } %>
          </div>
        </div>

      <% end %>

  <footer>
    <p>&copy; 2024 Planner IngUC. Todos los derechos reservados.</p>
    <p><a href="<%= "https://www.linkedin.com/in/sotomuena/" %>">Contáctanos</a> para más información.</p>
  </footer>

</body>
</html>
